#!/bin/sh

if [ "$WILDFLY_HOME" = "{{ wildfly_home }}" ]; then
    WILDFLY_HOME="{{ wildfly_home }}"
fi

if [[ "$1" == "domain" ]]; then
    $WILDFLY_HOME/bin/domain.sh -c $2 -b $3
else
    $WILDFLY_HOME/bin/standalone.sh -c $2 -b $3 -D$4
fi
